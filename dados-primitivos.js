const nome = 'Paulo';  //  type string
const num1 = 10;         //  type number
const num2 = 10.52;      //  type number
let nomeAluno;     // undefined -> não aponta para lugar nemhum da memoria
const sobrenome = null; // nulo -> não aponta para lugar nenhum da memoria
const aprovado = false; // Boolean = true, false  (lógico) 